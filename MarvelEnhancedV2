---- Script Made By Nut The Menace  ----------
---- Gui Produced by Nut The Menace -------

    if game.PlaceId == 6107938757 then
    local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/source.lua"))()
    local Window = Library.CreateLib("Marvel Enhanced V.2 GUI + More by Nut The Menace", "DarkTheme")
   
     -- Character Change
    local InvisibleWoman = Window:NewTab("Invisible Woman")
    local InvisibleWomanSection = InvisibleWoman:NewSection("Invisible Woman's & Scarlet Witch's Power Spam")
     
    InvisibleWomanSection:NewButton("Force Bash", "Power 1", function(txt)
    print("txt")
   local A_1 = "Invisible Woman"
   local Event = game:GetService("ReplicatedStorage").Remotes.Sound
   Event:InvokeServer(A_1) 
   local A_1 = "ShieldBash"
   local A_2 = CFrame.new(-10311.8096, 558.986633, -1250.23999, 0.99954164, -0.00034183127, -0.030272413, -2.91038305e-11, 0.999936223, -0.0112911221, 0.0302743427, 0.0112859467, 0.999477863)
   local Event = game:GetService("ReplicatedStorage").Remotes.InvisibleWomanAbilities
   Event:InvokeServer(A_1, A_2)
   
  end)






    InvisibleWomanSection:NewButton("Go Invisible", "Power 2", function(txt)
    print("txt")
    
    

local args = {
    [1] = "InvisibleStart",
    [2] = game:GetService("Players").LocalPlayer.Character
}

game:GetService("ReplicatedStorage").Remotes.InvisibleWomanAbilities:InvokeServer(unpack(args))

  end)

  


    InvisibleWomanSection:NewDropdown("Become Visible", "Invisibility OFF",{"Turns Invisiblity Off"}, function(currentOption)
    

local args = {
    [1] = "InvisibleEnd",
    [2] = game:GetService("Players").LocalPlayer.Character
}

game:GetService("ReplicatedStorage").Remotes.InvisibleWomanAbilities:InvokeServer(unpack(args))

end)


          

    


    InvisibleWomanSection:NewButton("Scarlet Witch's Flight ", "Flight effect", function(txt)
	print(txt)

    local A_1 = "HoverStart"
    local Event = game:GetService("ReplicatedStorage").Remotes.WandaAbilities
    Event:InvokeServer(A_1)

   
   
end) 






    InvisibleWomanSection:NewButton("QuickSilver's Speed Effect ", "Touch Effect", function(txt)
	print(txt)
    
   local A_1 = "StartSpeed"
   local Event = game:GetService("ReplicatedStorage").Remotes.QuicksilverAbilities
   Event:InvokeServer(A_1)
    

   
   
end) 



local Playerzz = Window:NewTab("Tk & Forcefield")
local PlayerzzSection = Playerzz:NewSection("Power Keybinds")

PlayerzzSection:NewKeybind("ForceField Someone", "MOTUS OP", Enum.KeyCode.E, function()
	print("You just clicked the bind")
local function getPlayer()
        local userInput = game:GetService("UserInputService")
        local cam = workspace.CurrentCamera
        local mousePos = userInput:GetMouseLocation()
        local mouseRay = cam:ViewportPointToRay(mousePos.X, mousePos.Y)
        players = {}
        for _, x in next, game:GetService('Players'):GetPlayers() do
            players[#players + 1] = x.Character
        end
        local raycastParams = RaycastParams.new()
        raycastParams.FilterType = Enum.RaycastFilterType.Whitelist
        raycastParams.FilterDescendantsInstances = players
        raycastParams.IgnoreWater = true
        local raycastResult = workspace:Raycast(mouseRay.Origin, mouseRay.Direction * 1000, raycastParams)
        if raycastResult then
            for _, x in next, game:GetService('Players'):GetPlayers() do
                if not raycastResult.Instance:IsDescendantOf(x.Character) then
                    continue 
                end
                return x
            end
        end
        return false
    end
    
    local function Set(list)
        local set = {}
        for _, l in ipairs(list) do
            if l then
                set[l] = true
            end
        end
        return set
    end
    game.Players.LocalPlayer:GetMouse().KeyDown:Connect(function(key)
     if key == "e" then
    local player = getPlayer()
     if player then
    -- Script generated by Nut The Menace Spy - credits to Claudev0#8849

local args = {
    [1] = "Shield",
    [2] = player.Character
}

game:GetService("ReplicatedStorage").Remotes.InvisibleWomanAbilities:InvokeServer(unpack(args))

    end
    end
    end)

end)


PlayerzzSection:NewKeybind("Hex Tk someone", "MOTUS OP", Enum.KeyCode.R, function()
	print("You just clicked the bind")
local function getPlayer()
        local userInput = game:GetService("UserInputService")
        local cam = workspace.CurrentCamera
        local mousePos = userInput:GetMouseLocation()
        local mouseRay = cam:ViewportPointToRay(mousePos.X, mousePos.Y)
        players = {}
        for _, x in next, game:GetService('Players'):GetPlayers() do
            players[#players + 1] = x.Character
        end
        local raycastParams = RaycastParams.new()
        raycastParams.FilterType = Enum.RaycastFilterType.Whitelist
        raycastParams.FilterDescendantsInstances = players
        raycastParams.IgnoreWater = true
        local raycastResult = workspace:Raycast(mouseRay.Origin, mouseRay.Direction * 1000, raycastParams)
        if raycastResult then
            for _, x in next, game:GetService('Players'):GetPlayers() do
                if not raycastResult.Instance:IsDescendantOf(x.Character) then
                    continue 
                end
                return x
            end
        end
        return false
    end
    
    local function Set(list)
        local set = {}
        for _, l in ipairs(list) do
            if l then
                set[l] = true
            end
        end
        return set
    end
    game.Players.LocalPlayer:GetMouse().KeyDown:Connect(function(key)
     if key == "r" then
    local player = getPlayer()
     if player then
    -- Script generated by Nut The Menace Spy - credits to Claudev0#8849

local args = {
    [1] = "HexTKStart",
    [2] = player.character.Humanoid
}

game:GetService("ReplicatedStorage").Remotes.WandaAbilities:InvokeServer(unpack(args))


    end
    end
    end)

end)

          

     -- PLAYER
    local Player = Window:NewTab("Player")
    local PlayerSection = Player:NewSection("Player")
    PlayerSection:NewButton("Run fast as speed", "You stole Speed's power", function()
    print(currentOption)
    while true do 
    wait(0,5)
    game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = 121

end
  
    
   end)
   
     PlayerSection:NewSlider("Jumper Power", "Changes Your JumpPower", 500, 20, function(s)
       game.Players.LocalPlayer.Character.Humanoid.JumpPower = s
   
  end)





  local Misc = Window:NewTab("Misc")
  local MiscSection = Misc:NewSection("Misc Options")
  MiscSection:NewButton("Remove Name", "Hides Name", function()
    print(currentOption)
    
    game.Players.LocalPlayer.Character.Name:destroy()
    end)


     
   

   
    -- Credits Info
    local Information = Window:NewTab("Info")
    local InformationSection = Information:NewSection("Information")




    
end 
